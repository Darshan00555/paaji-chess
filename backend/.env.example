# Backend server port (optional for Render, it injects PORT automatically)
PORT=3000

# Comma-separated allowed origins for Socket.IO CORS
CORS_ORIGIN=https://paaji-chess.pages.dev,https://your-custom-domain.com

# Legacy fallback origin key (optional)
FRONTEND_ORIGIN=https://paaji-chess.pages.dev
